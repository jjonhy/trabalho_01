#include <stdio.h>
#include <stdlib.h>
#include "data_manipulation.h"

//registro de cabeçalho

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	FILE* test;
	
	function1("tipo1","arquivoEntrada1.csv","saida.bin");
//	test = fopen("saida.bin","rb");
//	cab1 testano;
	
/*	fread(&testano,sizeof(cab1),1,test);
	
	printf("%c",testano.status);
	printf("%d",testano.topo);
	int i;
	for(i = 0; i<169;i++)
		printf("%c",testano.descricao[i]);
	printf("%d%d",testano.proxRRN,testano.nroRegRem);
*/			
	
			
	return 0;
}
